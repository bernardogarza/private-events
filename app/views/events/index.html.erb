<h1>Events</h1>

<% if flash[:success] %>
    <div class="notice"><%= flash[:success] %></div>
<% end %>

<% @events.each do |e| %>
    <h2><%= link_to  "Title:", event_path(e.id) %></h2>
    <h3><%= e.title %></h3>
    <h2>Date and time:</h2>
    <p><%= e.date %></p>
<% end %>

